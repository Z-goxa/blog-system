# Vercel 前端生产环境示例
#
# 推荐做法二选一：
#
# 方案 1：使用 Vercel rewrites 代理 /api 到后端。
# 这种情况下前端代码继续请求 /api，不需要设置 VITE_API_URL。
# 请在 frontend/vercel.json 中把 https://api.example.com 改成你的真实后端域名。

# VITE_API_URL=/api

# 方案 2：前端直接请求后端 API 域名。
# 这种情况下需要后端开启 CORS，允许你的 Vercel 域名访问。
# VITE_API_URL=https://api.example.com/api
